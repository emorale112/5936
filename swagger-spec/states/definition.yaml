type: object
title: States

properties:
  supported:
    type: array
    readOnly: true
    items:
      type: string
    description: 'The states Gusto currently supports.'
  unsupported:
    type: array
    readOnly: true
    items:
      type: string
    description: 'The states Gusto does not yet support.'
