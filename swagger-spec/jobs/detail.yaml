# /jobs/{job_id}
get:
  summary: Get an individual job
  description: >-
    Returns a single job representation.

    Jobs are essentially the intersection of an employee, a compensation, and a company work location.

  parameters:
    - in: path
      type: string
      required: true
      name: job_id
      description: 'The unique identifier of the job you wish to retrieve.'
  tags:
    - Jobs
  operationId: jobs_read
  x-response-schema: Job
  responses:
    '200':
      description: 'OK'
      schema:
        $ref: 'definition.yaml'
